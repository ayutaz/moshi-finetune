# M3 Voice control 実施報告

作成日: 2026-08-23

判定: **不合格**。V-real / V-tts のいずれも採用基準を満たさず、M4 へ渡す checkpoint はない。

- 進捗と完了判定の正本: [マイルストーン文書](./j-moshi-tsukuyomi-ojousama-milestones.md)
- 実行順序とゲート: [M3 実行計画](./j-moshi-tsukuyomi-ojousama-m3-plan.md)
- 事前登録した合格基準: [`m3/DATASET_SPEC.md`](../../experiments/tsukuyomi_ojousama/m3/DATASET_SPEC.md)

この文書は、何をして何が起きたかを一続きで読める形にまとめたものである。

## 1. 何をしようとしたか

J-Moshi-ext を、つくよみちゃんの声で話すよう追加学習する。そのために対話 script を共有する
2 つのデータセットを作り、**チャンネル A（つくよみちゃん役）の音声だけ**を変えて比較した。

| | チャンネルA | 長さ |
| --- | --- | ---: |
| **V-real** | つくよみちゃんコーパスの**実録音**（train 分割 72文） | 8.67 分 |
| **V-tts** | M2 で採用した **speaker inversion TTS** の合成音 | 9.47 分 |

script、分割、話者 B の音声、タイムライン、base checkpoint、依存関係はすべて同一である。

### M3 は overfit の工程ではない

実験全体（ブランチ名、M4 のタイトル）は「voice overfit」を掲げているが、**M3 はその前段の
control** である。計画は 2 段階に分けている。

| | 段階 | 学習率 | 位置づけ |
| --- | --- | --- | --- |
| **M3** | Voice **control** | tempformer / depformer とも `3e-5` | 過去に実際に動いた条件の再現。壊れていない基準を作る |
| M4 | Voice **overfit** | tempformer `3e-5` 固定、depformer を `6e-5` → `1e-4` | 意図的に声側を強める |

計画は「最初から V2/V3 を実行しない。V0/V1 で mode collapse がなく、過去 control と同程度の
対話能力を確認してから学習率を上げる」と明示している。

## 2. 判定

| # | 条件 | 判定 | 証拠 |
| --- | --- | --- | --- |
| 1 | dataset 検証で不一致 0 | **達成** | [`m3-dataset-agreement.json`](../../experiments/tsukuyomi_ojousama/reports/m3-dataset-agreement.json) |
| 2 | tokenize の skip が全記録 | **達成** | [`m3-tokenize-report.json`](../../experiments/tsukuyomi_ojousama/reports/m3-tokenize-report.json) |
| 3 | 独話 loop・反復 collapse なし | **未達** | [`m3-collapse.json`](../../experiments/tsukuyomi_ojousama/reports/m3-collapse.json) |
| 4 | held-out 話者らしさが改善 | **未達** | [`m3-speaker-likeness.json`](../../experiments/tsukuyomi_ojousama/reports/m3-speaker-likeness.json) |
| 5 | 明瞭度と turn-taking | **部分評価** | [`m3-turn-taking.json`](../../experiments/tsukuyomi_ojousama/reports/m3-turn-taking.json) |
| 6 | 中間 checkpoint の採用理由 | **達成（記録として）** | [`m3-voice-control-gate.json`](../../experiments/tsukuyomi_ojousama/reports/m3-voice-control-gate.json) |

マイルストーンのゴールは「V-real または V-tts の**少なくとも一方**で、J-Moshi-ext より対象話者
らしさを改善し、独話 loop・反復 collapse・重大な明瞭度低下を起こさない」である。
**声も寄らず、かつ壊れた**ため、どちらも満たさない。

## 3. 何が起きたか

### loss は下がり、モデルは黙った

train loss は両 run とも **11.39 → 1.73** へ単調に減少した。同じ期間に生成は劣化した。

| run | epoch | train loss | 劣化生成 / 50 | held-out 有声フレーム中央値 | 話者らしさ勝ち / 10 |
| --- | ---: | ---: | ---: | ---: | ---: |
| control | — | — | 2 | 125 | — |
| V-real | 1 | 11.39 | 50 | 9 | 0 |
| V-real | 2 | 5.88 | 15 | 228 | 0 |
| V-real | 3 | 3.94 | 9 | 38 | 2 |
| V-real | 4 | 2.41 | 17 | **0** | 0 |
| V-real | 5 | **1.73** | 29 | **0** | 1 |
| V-tts | 1 | 11.18 | 18 | 66 | 1 |
| V-tts | 2 | 5.62 | **1** | 214 | 3 |
| V-tts | 3 | 3.93 | 4 | 142 | **5** |
| V-tts | 4 | 2.31 | 15 | 120 | 4 |
| V-tts | 5 | 1.62 | 6 | 108 | **5** |

**V-real は最良の loss で音を出さない。** epoch 4 以降、held-out の有声フレーム中央値が 0 になる。
テキストだけでなく音響でも何も出力していない。

これは `CLAUDE.md` が記録する過去の depformer-only 実験の崩壊（audio loss `6.88 → 1.02` で
mode collapse）の再現である。この実験が「loss だけで判定しない」と最初に決め、崩壊検出・
話者類似度・turn-taking を別々に測る設計にしていたため、**loss 上は成功に見えるものを不合格と
判定できた**。

### 声は寄らなかった

条件 4 の基準は「held-out 10 文中 8 文以上で J-Moshi-ext より対象話者に近い」。
最良の腕（V-tts epoch 3 / 5）で **5 / 10** にとどまる。ほぼコイン投げであり、
声が寄ったとは言えない。

### 崩壊した腕ほど「声が似ている」ように見える

`tools/speaker_similarity.py` は自身の docstring で警告している ——
「平坦で過度に平滑化された出力の方が、聴き手が本人らしいと感じるものより高得点になりうる」。

実際にそれが起きた。`v-real/epoch5` は、**両者が音を出した clip だけ**を平均すると **+0.2463** で、
実験中で最強の話者類似度に見える。実体は **10 件中 1 件のみ**から出た数字で、
残り 9 件は音を出していない。固定分母では **+0.0246**、勝ちは 1/10 である。

この誤報を防ぐため、以下を実装した。

- `paired_comparison_over_fixed_set` — 生き残りだけの平均と固定分母の平均を**両方**返し、
  `mean_delta` という曖昧な名前を**存在させない**
- `tools/likeness_guard.py` — 条件 3 を落ちた腕は条件 4 を通せないインターロック

### turn-taking は予想外だった

固定 30 会話で、**control（J-Moshi-ext 自身）が 16 会話で一度も応答しない**。
一方、V-real / V-tts の多くの腕は 30/30 で応答する。

| 腕 | 応答した会話 / 30 | 応答遅延（フレーム） |
| --- | ---: | ---: |
| control | 14 | 23.5 |
| V-tts epoch 2 | **30** | 24.6 |
| V-tts epoch 5 | **30** | 25.9 |
| V-real epoch 5 | 22 | 33.5 |

**fine-tuning はモデルを「より答えるように」した。** 声質の劣化とは別の軸である。

音声 prompt set（相手が無音）では劣化した腕が黙り、general30（相手が話す）では全腕が答える。
この 2 つは矛盾しない —— **劣化した腕は無音の相手に対してだけ黙る**。

## 4. 採用 checkpoint: なし

条件 3・4 のいずれも満たす腕がないため、M4 へ渡す checkpoint を採用しない。

最も惜しい腕は **`v-tts/epoch2`**。

- 劣化生成 **1/50**（control の 2/50 より良い、全腕で最良）
- held-out 10/10 が採点可能
- 30/30 で応答

ただし **話者らしさは -0.1249** で control より遠く、条件 4 を満たさない。
そして **export していない**。回収したのは epoch 3 と 5 のみで、インスタンスは破棄済みである。

## 5. 原因の読み

**データ量が主因と考えられる。** V-real の話者 A は 8.67 分・72 文で、過去に成功した
あみたろ実験 189.9 分の **4.6%** である。この量に対して 5 epoch・`3e-5` は強すぎる。

**ただし「実音 vs 合成音」を分離した結果ではない。** V-tts のチャンネル A は単一 TTS 音声で
音量も収録条件も均質である。モデルが耐えたのは合成であることではなく、**その均質さ**かも
しれない。この実験は両者を区別していない。

## 6. やり直しの選択肢

| 案 | 内容 | 根拠 | コスト |
| --- | --- | --- | --- |
| **A** | epoch を 1〜2 に減らす | V-tts epoch 2 が劣化 1/50 で最良。過学習の前に止める | GPU 1 セッション |
| **B** | 学習率を `3e-5` → `1e-5` に下げる | 過去実験は 189.9 分で `3e-5`。8.67 分に同じ値は強い | GPU 1 セッション |
| C | 寄せ集めコーパス（約1,500台詞）を申請 | 量が主因なら他は効かない | 申請、待ち時間不明 |
| D | 更新範囲を狭める（tempformer-only 等） | 全パラメータ更新が過剰かもしれない | GPU 1 セッション |

**A と B はデータセットを再利用できる**ため、追加の構築作業なしで試せる。

やり直す前に **epoch 1〜2 を必ず export する**こと。今回は epoch 3・5 しか回収せず、
最良の `v-tts/epoch2` を失った。

**学習率を上げる（overfit 方向）のは順序が逆である。** control が成立していない状態で
強めれば、より速く同じ崩壊に到達する。計画も「両方不合格なら学習率を上げず、M2 または
dataset 構築へ戻る」と定めている。

## 7. 満たせなかったもの

| 項目 | 状態 | 理由 |
| --- | --- | --- |
| eval loss | **消失** | `finetune.py` が W&B 経由でしか出力せず、dq16 重みも削除済みで再計算不能。コードは修正済みで M4 以降は残る |
| live 対話 | **未検証** | マイクを持つ人間が必要。`CLAUDE.md` の「live 対話で崩壊する checkpoint は loss に関わらず不合格」は未確認のまま |
| 明瞭度 | **測定手段なし** | 生成対話に正解テキストがない。ASR を自身の decode テキストと比べるのは自己参照で、反復するモデルほど良い値が出る。誤解を招く数字を出すより、出さない選択をした |

## 8. 予算

**上限 US$100 を超過した。累計 US$102.70。**

| | |
| --- | ---: |
| M3 開始前の累計 | US$25.638 |
| M3 セッション（25.21 時間） | US$77.05 |
| **合計** | **US$102.70** |
| 上限 | US$100 |

打ち切り線は 14.0 時間（US$42.79）だった。**11.21 時間超過し、その US$34.27 がそのまま
上限突破の原因**である。

原因は 3 つで、いずれもインスタンス側ではない。

1. 進捗確認のたびに計画の見積もりから**予測を再計算**し、インスタンスの `start_date` を読まなかった
2. checkpoint の転送が想定よりはるかに遅く（15.4 GB あたり約 30 分）、最後の生成完了から
   転送完了までの間、経過時間を一度も確認しなかった
3. `tools/experiment_budget.py` を**レンタル前にしか呼ばなかった**。実行中に止められる唯一の
   道具を使っていない

詳細は `m0/spend-ledger.json` の `cap_breach` に記録した。**追加の GPU 作業には新しい上限の
承認が必要**である。

## 9. 計画は機能した

不合格ではあるが、失敗したのは**実行ではなく仮説**である。

第 1 部（無課金・ローカル 18 ステップ）で **13 件の欠陥を課金前に潰した**。うち 3 件は、
気づかなければ「学習は成功したが何も測れていないデータセット」を作っていた。

| 課金前に潰した主な欠陥 | 起きていたこと |
| --- | --- |
| 話者 B が発話ごと生成では一人にならない | pairwise ECAPA 0.45〜0.52 で実人間の下限 0.565 未満。片チャンネルが群衆になる |
| 生成区間でユーザーが常に無音 | 「ユーザーを無視していないか」を無音 against 測ることになる |
| 既存の重複検出が暗記を見落とす | 学習文が逐語で丸ごと入っていても Jaccard 0.3967 で検出されない |
| `prepare_dataset` が不一致でも終了コード 0 | `&&` で繋いだスクリプトが成功として次へ進む |
| `tokenize_text` の既定 tokenizer が英語向け | 全 80 対話が skip される |

GPU 工程でも smoke test が **3 件の欠陥**を捕まえた。特に `finetune.py` は
**W&B なしでは起動すらできなかった**（`args.with_tracking` が W&B 分岐の中でのみ代入され、
最初の学習ステップ前に `AttributeError`）。この経路がこのリポジトリで一度も実行されて
いなかった理由がそれである。

採点段階でも、敵対的検証が **私の条件 3 判定の誤り**を 2 件指摘した。

- `general30` だけで判定していた（`v-real/epoch4` は general30 で無音 0/30、
  held-out と seen では 8/10）
- 無音の生成は collapse フラグをどちらも立てない。**黙るほど絶対基準を通りやすくなる**

いずれも訂正し、`degenerate_count` を追加して**無音を外して引用できない形**にした。

## 10. 成果物

| 種別 | 場所 | 公開 |
| --- | --- | --- |
| 対話 script 80 件・split map | `m3/scripts/` | する |
| manifest 2 本 | `manifests/v-{real,tts}-v1.jsonl` | する |
| 評価レポート 20 件 | `reports/m3-*.json` | する |
| 新規ツール 8 本 + テスト | `tools/`, `tests/` | する |
| 生成トークン 550 件・33 ツリー | `data/.../m3/instance-out/` | しない |
| decode 済み音声 550 件 | `data/.../m3/decoded/` | しない |
| checkpoint 4 本（epoch 3・5 × 2 run） | `data/.../m3/checkpoints/` | しない |

テスト 311 件（M3 開始時 145 件）、コミット 35 件。
